import type { GetProvidersSnapshotResponseMessage, SessionOutboundMessage } from "@getpaseo/protocol/messages";
type Update = Extract<SessionOutboundMessage, {
    type: "providers_snapshot_update";
}>;
type Snapshot = GetProvidersSnapshotResponseMessage["payload"];
/** Resolve announcements only while observed, coalescing changes during a fetch. */
export declare class ProviderSnapshotUpdates {
    private readonly host;
    private pending;
    private paused;
    constructor(host: {
        fetch(cwd: string | undefined): Promise<Snapshot>;
        emit(update: Update): void;
        failed(error: unknown): void;
    });
    clear(): void;
    pause(): void;
    resume(): void;
    receive(update: Update): void;
    private resolve;
}
export {};
//# sourceMappingURL=index.d.ts.map