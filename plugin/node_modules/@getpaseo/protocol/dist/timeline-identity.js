export function timelineItemIdentity(item) {
    if (item.type === "tool_call")
        return item.callId;
    if (item.type === "plugin")
        return `${item.pluginId}/${item.id}`;
    return null;
}
//# sourceMappingURL=timeline-identity.js.map