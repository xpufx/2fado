{
  "_comment": "mode blacklist: whitelist runs, blacklist denies, rest -> default(ask|deny). mode whitelist: whitelist runs, EVERYTHING else denies (blacklist+default ignored). Exact-argv matches only.",
  "mode": "blacklist",
  "whitelist": [],
  "blacklist": [],
  "confirm": [
    [
      "rm",
      "-rf",
      "/"
    ],
    [
      "/bin/echo",
      "dangerous"
    ]
  ],
  "default": "ask",
  "env_keep": []
}